// $ This hook takes in the skills data and return the unique list of skills in the dataset

import mySkillsData from "../../public/data/mySkillsData";
import useSingleCategoriesHook from "./useSingleCategoriesHook";

// const useCategoryLanguageList = () => {
//   const { uniqueCategories } = useSingleCategoriesHook();

//   // $ This hook takes in the skills data and filter out the languages that belong to the category passed in from the uniqueCategories array

//   {
//     uniqueCategories.map((category: string) => {
//       const filteredLanguageList = mySkillsData.filter(
//         (item) => item.category === category
//       );
//     });
//   }
//   return { filteredLanguageList };
// };

// export default useCategoryLanguageList;
